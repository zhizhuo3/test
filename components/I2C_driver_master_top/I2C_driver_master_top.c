#include <stdio.h>
#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "esp_log.h"
#include "driver/i2c.h"
#include "I2C_driver_master_top.h"
#include "soft_i2c.h"
#include "esp_check.h"


const char* EXAMPLE_TAG = "soft_i2c";
硬件I2C模块驱动////////////////////////////////////////////////////////////////////////////////
//硬件IIC初始化

void i2c_master_init() {
    static i2c_config_t conf;
    conf.mode = I2C_MODE_MASTER;
    conf.sda_io_num = I2C_MASTER_SDA_IO;
    conf.sda_pullup_en = GPIO_PULLUP_ENABLE;
    conf.scl_io_num = I2C_MASTER_SCL_IO;
    conf.scl_pullup_en = GPIO_PULLUP_ENABLE;
    conf.master.clk_speed = 400000; // 400kHz
    esp_err_t ret = i2c_param_config(I2C_MASTER_NUM, &conf);
    ESP_ERROR_CHECK(ret);
    ret = i2c_driver_install(I2C_MASTER_NUM, conf.mode, 0, 0, 0);
    ESP_ERROR_CHECK(ret);
}

//硬件IIC2初始化
void i2c2_master_init() {
    i2c_config_t conf;
    conf.mode = I2C_MODE_MASTER;
    conf.sda_io_num = I2C2_MASTER_SDA_IO;
    conf.sda_pullup_en = GPIO_PULLUP_ENABLE;
    conf.scl_io_num = I2C2_MASTER_SCL_IO;
    conf.scl_pullup_en = GPIO_PULLUP_ENABLE;
    conf.master.clk_speed = 400000; // 400kHz
    esp_err_t ret = i2c_param_config(I2C2_MASTER_NUM, &conf);
    ESP_ERROR_CHECK(ret);
    ret = i2c_driver_install(I2C2_MASTER_NUM, conf.mode, 0, 0, 0);
    ESP_ERROR_CHECK(ret);
}


//////////////////////////////////////////////////////////////////////////////////硬件I2C模块驱动软件I2C模块驱动/////////////////////////////////////////////////////////////////有问题后需要检查


//软件IIC3初始化
void i2c3_master_init() {
myI2C_Init();
}

//软件IIC4初始化
void i2c4_master_init() {
myI2C_2_Init();
}

//////////////////////////////////////////////////////////////////////////////////软件I2C模块驱动

